# GEO-SCOPE Release Server Environment Configuration
# 复制此文件为 .env 并修改配置

# =============================================================================
# 服务器配置
# =============================================================================
HOST=0.0.0.0
PORT=8001
DEBUG=false

# =============================================================================
# 数据库配置
# =============================================================================
# SQLite (默认)
DATABASE_URL=sqlite:///data/releases.db

# PostgreSQL (生产环境推荐)
# DATABASE_URL=postgresql://user:password@localhost:5432/geo_scope_releases

# =============================================================================
# API 认证
# =============================================================================
# 如果未设置，服务器启动时会自动生成随机密钥
RELEASE_API_KEY=your-secure-api-key-here

# =============================================================================
# Beta 测试通道
# =============================================================================
# 多个密钥用逗号分隔
BETA_ACCESS_KEYS=geo-scope-beta-2025,internal-tester-key

# =============================================================================
# 文件存储
# =============================================================================
# PACKAGES_DIR=./packages
# ASSETS_DIR=./assets

# 头像上传限制 (字节)
# MAX_AVATAR_SIZE=2097152

# =============================================================================
# CORS 配置
# =============================================================================
# 多个域名用逗号分隔，* 表示允许所有
CORS_ORIGINS=*
